{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"kubectl-audit \u2014 Kubernetes Cluster Health Plugin","text":"kubectl-audit <p>Kubernetes cluster health, straight from your terminal.</p> kubectl krew install codenio/audit <p><code>kubectl-audit</code> is a <code>kubectl</code> plugin that surfaces Kubernetes resources failing common health checks \u2014 without switching tools or writing custom scripts.</p> <p>Use it for incident triage, SRE runbooks, pre-deploy smoke checks, and CI/CD gates.</p>"},{"location":"#what-it-checks","title":"What it checks","text":"Resource Flagged when\u2026 Pods Phase \u2260 <code>Running</code>, any container not <code>Ready</code>, or restart count \u2265 5 Containers Per-container: image pull errors, crash loops, not ready, high restarts Nodes <code>NotReady</code> or <code>SchedulingDisabled</code> (cordoned) PV / PVC Not in <code>Bound</code> phase (<code>Pending</code>, <code>Lost</code>, <code>Released</code>, <code>Failed</code>) Jobs Failed \u2014 including backoff and deadline limit failures CronJobs <code>suspend: true</code> Services Non-empty selector with no matching Pods in the namespace Deployments <code>spec.replicas: 0</code>, or <code>readyReplicas</code> &lt; desired Events <code>type: Warning</code> <p>See Audit Rules for exact detection thresholds.</p>"},{"location":"#navigate-the-docs","title":"Navigate the docs","text":"\ud83d\ude80 Installation <p>Krew, from source, verify</p> \u26a1 Quick Start <p>Common workflows and first commands</p> \ud83d\udccb Audit Rules <p>Every check, threshold, and detection logic</p> \ud83d\udd27 Subcommands <p>Full subcommand and flag reference</p> \ud83d\udd0d Troubleshooting <p>Common errors and fixes</p> \ud83d\udd04 CI/CD Integration <p>GitHub Actions, exit codes, scripting</p>"},{"location":"contributing/","title":"Contributing","text":"<p>Issues and pull requests are welcome at github.com/codenio/kubectl-audit/issues.</p>"},{"location":"contributing/#development-setup","title":"Development setup","text":"<p>Requirements: Go 1.21+, <code>make</code>, a working Kubernetes cluster context.</p> <pre><code>git clone https://github.com/codenio/kubectl-audit.git\ncd kubectl-audit\n\nmake bin          # build bin/audit\nmake test         # run tests + generate coverage profile\nmake fmt          # format code\nmake vet          # vet code\nmake install      # install to ~/.krew/bin/kubectl-audit\n</code></pre> <p>Run without installing:</p> <pre><code>go run ./cmd/plugin --help\ngo run ./cmd/plugin pods\ngo run ./cmd/plugin containers --help\n</code></pre>"},{"location":"contributing/#before-opening-a-pr","title":"Before opening a PR","text":"<ul> <li>[ ] <code>make test</code> passes</li> <li>[ ] <code>make fmt</code> and <code>make vet</code> are clean (<code>make bin</code> runs both)</li> <li>[ ] Validated against a real cluster with <code>make install &amp;&amp; kubectl audit \u2026</code></li> </ul>"},{"location":"contributing/#repository-layout","title":"Repository layout","text":"Path Purpose <code>cmd/plugin/</code> Entrypoint and CLI (Cobra, config flags, printing) <code>pkg/plugin/</code> Audit logic, container list/table data, server-side table handling <code>deploy/krew/plugin.yaml</code> Krew manifest template for releases <code>examples/audit-*/</code> Demo manifests per audit type <code>docs/</code> MkDocs documentation source"},{"location":"contributing/#releasing","title":"Releasing","text":"<p>Releases are automated via GitHub Actions on <code>v*.*.*</code> tags. See <code>.github/workflows/release.yml</code>.</p>"},{"location":"contributing/#bumping-kubernetes-dependencies","title":"Bumping Kubernetes dependencies","text":"<pre><code>make kubernetes-deps\n</code></pre>"},{"location":"contributing/#reporting-bugs","title":"Reporting bugs","text":"<p>Please include:</p> <ul> <li>Plugin version (binary or release tag)</li> <li><code>kubectl version</code> output</li> <li>Exact command and full error message</li> <li>Kubernetes distribution and version (EKS, GKE, k3s, etc.)</li> </ul> <p>Open an issue: github.com/codenio/kubectl-audit/issues/new</p>"},{"location":"examples/","title":"Examples","text":"<p>Each subcommand has a demo manifest under <code>examples/</code> that creates intentionally broken resources \u2014 so you can see the plugin in action without needing a broken cluster.</p>"},{"location":"examples/#all-demos","title":"All demos","text":"Example Subcommand Demo folder Pods <code>kubectl audit pods</code> <code>examples/audit-pods/</code> Containers <code>kubectl audit containers</code> <code>examples/audit-containers/</code> Jobs &amp; CronJobs <code>kubectl audit jobs</code> / <code>cronjobs</code> <code>examples/audit-job/</code>, <code>audit-cronjob/</code> Services <code>kubectl audit service</code> <code>examples/audit-svc/</code> Deployments <code>kubectl audit deploy</code> <code>examples/audit-deploy/</code> PV &amp; PVC <code>kubectl audit pv</code> / <code>pvc</code> <code>examples/audit-pv/</code>, <code>audit-pvc/</code> Events <code>kubectl audit events</code> <code>examples/audit-events/</code> Nodes <code>kubectl audit nodes</code> (cluster-specific, no demo)"},{"location":"examples/#demo-pattern","title":"Demo pattern","text":"<p>Every demo follows this three-step pattern:</p> <pre><code># 1. Create the broken resources\nkubectl apply -f examples/audit-&lt;target&gt;/demo.yaml\n\n# 2. Run the audit\nkubectl audit &lt;target&gt; -n kubectl-audit-&lt;target&gt;-demo\n\n# 3. Clean up\nkubectl delete -f examples/audit-&lt;target&gt;/demo.yaml\n</code></pre>"},{"location":"examples/#reading-the-output","title":"Reading the output","text":"<p>Every audit prints a summary to stderr: <pre><code>-------------------------------------------------------\nPod Audit summary: total = 9 benign = 3 attention = 6\n-------------------------------------------------------\n</code></pre></p> <ul> <li>total \u2014 all resources of that type found in scope</li> <li>benign \u2014 resources passing all checks</li> <li>attention \u2014 resources with at least one failing check</li> </ul> <p>The table that follows lists only the attention resources. If <code>attention = 0</code>, there is nothing to report and no table is printed.</p>"},{"location":"examples/containers/","title":"Containers","text":"<p><code>kubectl audit containers</code> gives per-container visibility \u2014 one row per container (init or app), showing exactly which container inside a pod is broken.</p> <p>Use this after <code>kubectl audit pods</code> shows a pod in a bad state but you need to know which specific container is the cause.</p>"},{"location":"examples/containers/#demo","title":"Demo","text":"<pre><code>kubectl apply -f examples/audit-containers/demo.yaml\nkubectl audit containers -n kubectl-audit-containers-demo -l demo=kubectl-audit-containers\nkubectl delete -f examples/audit-containers/demo.yaml\n</code></pre>"},{"location":"examples/containers/#sample-output","title":"Sample output","text":"DefaultWide (-o wide) <pre><code>$ kubectl audit containers -n demo\n-------------------------------------------------------\nContainer Audit summary: total = 12 benign = 9 attention = 3\n-------------------------------------------------------\nContainer that requires attention\nPOD                     NAME       READY   STATUS             RESTARTS   AGE   TYPE\nworkload-dep-abc-xyz    sidecar    0       ImagePullBackOff   0          -     container\nworkload-dep-abc-xyz    init-cfg   -       Failed             2          -     init-container\n</code></pre> <pre><code>$ kubectl audit containers -n demo -o wide\nPOD                     NAME       READY   STATUS             RESTARTS   AGE   TYPE            PORTS   IMAGE                     PULLPOLICY\nworkload-dep-abc-xyz    sidecar    0       ImagePullBackOff   0          -     container               myrepo/sidecar:bad-tag    IfNotPresent\n</code></pre>"},{"location":"examples/containers/#scope-to-a-single-pod","title":"Scope to a single pod","text":"<pre><code>kubectl audit containers -p my-pod-0\nkubectl audit containers --pod my-pod-0 -n my-namespace\n</code></pre>"},{"location":"examples/containers/#output-formats","title":"Output formats","text":"Format Description (default) Fixed table: <code>POD</code>, <code>NAME</code>, <code>READY</code>, <code>STATUS</code>, <code>RESTARTS</code>, <code>AGE</code>, <code>TYPE</code> <code>-o wide</code> Adds <code>PORTS</code>, <code>IMAGE</code>, <code>PULLPOLICY</code> <code>-o json</code> Full JSON <code>-o yaml</code> Full YAML <code>-o name</code> <code>namespace/pod-name</code> lines <p>Warning</p> <p><code>custom-columns</code>, <code>jsonpath</code>, and Go templates are not supported for <code>containers</code>. Use <code>-o json | jq</code> instead.</p>"},{"location":"examples/containers/#next-steps","title":"Next steps","text":"<ul> <li>See detection rules \u2192 Audit Rules: Containers</li> <li>Aggregate pod view \u2192 kubectl audit pods</li> </ul>"},{"location":"examples/deployments/","title":"Deployments","text":"<p><code>kubectl audit deploy</code> finds Deployments that are scaled to zero or have fewer ready replicas than desired.</p>"},{"location":"examples/deployments/#demo","title":"Demo","text":"<pre><code>kubectl apply -f examples/audit-deploy/demo.yaml\nkubectl audit deploy -n kubectl-audit-deploy-demo\nkubectl delete -f examples/audit-deploy/demo.yaml\n</code></pre>"},{"location":"examples/deployments/#sample-output","title":"Sample output","text":"<pre><code>$ kubectl audit deploy -A\n-------------------------------------------------------\nDeployment Audit summary: total = 30 benign = 27 attention = 3\n-------------------------------------------------------\nDeployment that requires attention\nNAMESPACE   NAME               READY   UP-TO-DATE   AVAILABLE   AGE\napp-ns      web-frontend       0/3     3            0           2h\nops-ns      batch-worker       0/0     0            0           5d\nstaging     api-gateway        1/3     3            1           45m\n</code></pre>"},{"location":"examples/deployments/#detection-rules","title":"Detection rules","text":"Condition Flagged <code>spec.replicas == 0</code> \u2705 Explicitly scaled to zero <code>spec.replicas</code> unset and <code>readyReplicas &lt; 1</code> \u2705 API default is 1 <code>readyReplicas &lt; spec.replicas</code> when <code>spec.replicas &gt; 0</code> \u2705 Rollout not complete or replicas crashing <code>readyReplicas == spec.replicas &gt; 0</code> \u274c Healthy"},{"location":"examples/deployments/#useful-commands","title":"Useful commands","text":"<pre><code>kubectl audit deploy -A\nkubectl audit deploy -n my-namespace -l app=web\nkubectl audit deploy -o json | jq '.items[] | {name: .metadata.name, ready: .status.readyReplicas}'\n</code></pre>"},{"location":"examples/deployments/#next-steps","title":"Next steps","text":"<ul> <li>Deployment unhealthy? Check the pods \u2192 kubectl audit pods</li> <li>See full rules \u2192 Audit Rules: Deployments</li> </ul>"},{"location":"examples/events/","title":"Events","text":"<p><code>kubectl audit events</code> lists all events with <code>type: Warning</code>. Normal events are excluded \u2014 only Warning events signal potential problems.</p>"},{"location":"examples/events/#demo","title":"Demo","text":"<pre><code>kubectl apply -f examples/audit-events/demo.yaml\nkubectl audit events -n kubectl-audit-events-demo\nkubectl delete -f examples/audit-events/demo.yaml\n</code></pre>"},{"location":"examples/events/#sample-output","title":"Sample output","text":"<pre><code>$ kubectl audit events -A\n-------------------------------------------------------\nEvent Audit summary: total = 156 benign = 143 attention = 13\n-------------------------------------------------------\nEvent that requires attention\nNAMESPACE   LAST SEEN   TYPE      REASON              OBJECT                          MESSAGE\napp-ns      5m          Warning   BackOff             Pod/web-dep-abc-xyz             Back-off restarting failed container\nops-ns      2m          Warning   FailedScheduling    Pod/batch-job-pod               0/3 nodes available: Insufficient cpu\nstaging     12m         Warning   Unhealthy           Pod/api-dep-rst-uvw             Liveness probe failed: ...\n</code></pre>"},{"location":"examples/events/#common-warning-reasons","title":"Common warning reasons","text":"Reason What it means <code>BackOff</code> Container is crash-looping <code>FailedScheduling</code> No node can satisfy the Pod's resource/affinity requirements <code>Unhealthy</code> Liveness or readiness probe failing <code>OOMKilled</code> Container exceeded memory limit <code>FailedMount</code> Volume couldn't be mounted <code>ImagePullBackOff</code> Image pull failed after retries"},{"location":"examples/events/#useful-commands","title":"Useful commands","text":"<pre><code>kubectl audit events -A               # cluster-wide warning events\nkubectl audit ev -n my-namespace      # namespace-scoped\nkubectl audit events -o wide          # includes source, count columns\nkubectl audit events -o json | jq -r '.items[] | \"\\(.reason): \\(.message)\"'\n</code></pre>"},{"location":"examples/events/#next-steps","title":"Next steps","text":"<ul> <li><code>BackOff</code> events \u2192 investigate with kubectl audit pods and kubectl audit containers</li> <li><code>FailedScheduling</code> \u2192 check kubectl audit nodes for unschedulable nodes</li> <li>See detection rules \u2192 Audit Rules: Events</li> </ul>"},{"location":"examples/jobs/","title":"Jobs &amp; CronJobs","text":""},{"location":"examples/jobs/#jobs","title":"Jobs","text":"<p><code>kubectl audit jobs</code> lists Jobs with <code>status.failed &gt; 0</code> \u2014 including those that hit their backoff limit or active deadline.</p>"},{"location":"examples/jobs/#demo","title":"Demo","text":"<pre><code>kubectl apply -f examples/audit-job/demo.yaml\nkubectl audit jobs -n kubectl-audit-job-demo\nkubectl delete -f examples/audit-job/demo.yaml\n</code></pre>"},{"location":"examples/jobs/#sample-output","title":"Sample output","text":"<pre><code>$ kubectl audit jobs -A\n-------------------------------------------------------\nJob Audit summary: total = 28 benign = 26 attention = 2\n-------------------------------------------------------\nJob that requires attention\nNAMESPACE    NAME               COMPLETIONS   DURATION   AGE\nbatch-demo   daily-import       0/1           5m         5m\nbatch-demo   retry-migrate      0/1           1h         1h\n</code></pre>"},{"location":"examples/jobs/#useful-commands","title":"Useful commands","text":"<pre><code>kubectl audit jobs -A\nkubectl audit job -n batch-ns\nkubectl audit jobs -o json | jq '.items[].metadata.name'\n</code></pre>"},{"location":"examples/jobs/#cronjobs","title":"CronJobs","text":"<p><code>kubectl audit cronjobs</code> lists CronJobs where <code>spec.suspend = true</code>. Suspended CronJobs will not create new Jobs until unsuspended.</p>"},{"location":"examples/jobs/#demo_1","title":"Demo","text":"<pre><code>kubectl apply -f examples/audit-cronjob/demo.yaml\nkubectl audit cronjobs -n kubectl-audit-cronjob-demo\nkubectl delete -f examples/audit-cronjob/demo.yaml\n</code></pre>"},{"location":"examples/jobs/#sample-output_1","title":"Sample output","text":"<pre><code>$ kubectl audit cronjobs -A\n-------------------------------------------------------\nCronJob Audit summary: total = 15 benign = 13 attention = 2\n-------------------------------------------------------\nCronJob that requires attention\nNAMESPACE   NAME               SCHEDULE      SUSPEND   ACTIVE\nops-demo    pause-backup       0 2 * * *     True      0\nops-demo    hold-reports       15 * * * *    True      0\n</code></pre>"},{"location":"examples/jobs/#useful-commands_1","title":"Useful commands","text":"<pre><code>kubectl audit cj -A\nkubectl audit cronjobs -n ops-ns\n</code></pre>"},{"location":"examples/jobs/#next-steps","title":"Next steps","text":"<ul> <li>See detection rules \u2192 Audit Rules: Jobs / CronJobs</li> <li>Failed Job? Check the pods it created:   <pre><code>kubectl audit pods -n &lt;namespace&gt; -l job-name=&lt;job-name&gt;\n</code></pre></li> </ul>"},{"location":"examples/nodes/","title":"Nodes","text":"<p><code>kubectl audit nodes</code> lists nodes that cannot be scheduled to \u2014 either because they are <code>NotReady</code> or because they have been cordoned (<code>SchedulingDisabled</code>).</p> <p>Nodes are cluster-scoped; <code>-n</code> and <code>-A</code> do not apply.</p> <p>Info</p> <p>There is no demo manifest for nodes \u2014 node states are cluster-specific. Run against your own cluster.</p>"},{"location":"examples/nodes/#sample-output","title":"Sample output","text":"<pre><code>$ kubectl audit nodes\n-------------------------------------------------------\nNode Audit summary: total = 66 benign = 46 attention = 20\n-------------------------------------------------------\nNode that requires attention\nNAME            STATUS                        ROLES    AGE     VERSION\ndefault-0       Ready,SchedulingDisabled      &lt;none&gt;   459d    v1.30.3\ndefault-1       Ready,SchedulingDisabled      &lt;none&gt;   35d     v1.30.3\nworker-0        NotReady,SchedulingDisabled   &lt;none&gt;   2d21h   v1.33.3\nworker-1        NotReady,SchedulingDisabled   &lt;none&gt;   23h     v1.33.3\nworker-3        NotReady                      &lt;none&gt;   24h     v1.33.3\n</code></pre> <p><code>Ready,SchedulingDisabled</code> \u2014 node is healthy but cordoned (e.g., draining for maintenance). No new Pods will be scheduled here. <code>NotReady</code> \u2014 node is not reporting healthy to the control plane. Pods on this node may be evicted. <code>NotReady,SchedulingDisabled</code> \u2014 both: node is down and cordoned.</p>"},{"location":"examples/nodes/#useful-commands","title":"Useful commands","text":"<pre><code>kubectl audit nodes\nkubectl audit nodes -o json | jq '.items[].metadata.name'\nkubectl audit nodes -o wide\n</code></pre>"},{"location":"examples/nodes/#next-steps","title":"Next steps","text":"<ul> <li>See detection rules \u2192 Audit Rules: Nodes</li> <li>Cordoning a node: <code>kubectl cordon &lt;node-name&gt;</code></li> <li>Draining a node: <code>kubectl drain &lt;node-name&gt; --ignore-daemonsets</code></li> </ul>"},{"location":"examples/pods/","title":"Pods","text":"<p><code>kubectl audit pods</code> lists pods flagged by health checks \u2014 see Audit Rules: Pods for the exact criteria.</p>"},{"location":"examples/pods/#demo","title":"Demo","text":"<pre><code>kubectl apply -f examples/audit-pods/demo.yaml\nkubectl audit pods -n kubectl-audit-pods-demo -l demo=kubectl-audit-pods\nkubectl delete -f examples/audit-pods/demo.yaml\n</code></pre>"},{"location":"examples/pods/#sample-output","title":"Sample output","text":"Single namespaceAll namespaces <pre><code>$ kubectl audit pods -n ns-prod\n-------------------------------------------------------\nPod Audit summary: total = 9 benign = 3 attention = 6\n-------------------------------------------------------\nPod that requires attention\nNAME                                            READY   STATUS                  RESTARTS   AGE\nworkload-a-dep-54b6948c9c-pqx12                 0/1     ImagePullBackOff        0          8h\nworkload-a-dep-785b496f5d-rst34                 0/1     ContainerCreating       0          11h\nsvc-b-dep-6c87c74674-uvw56                      2/3     ErrImagePull            0          3h5m\nsvc-b-dep-9f6d7b5cc-xyz99                       0/3     ContainerStatusUnknown  0          12h\nsidecar-c-dep-9c5c948dd-abccd                   0/1     CrashLoopBackOff        0          18h\nsidecar-c-dep-c45d7c6c5-efghi                   0/1     InvalidImageName        0          8h\n</code></pre> <pre><code>$ kubectl audit pods -A\n-------------------------------------------------------\nPod Audit summary: total = 120 benign = 114 attention = 6\n-------------------------------------------------------\nPod that requires attention\nNAMESPACE      NAME                                            READY   STATUS             RESTARTS   AGE\nns-team-a      workload-dep-5f7677d8c-pqx12                    0/1     ImagePullBackOff   0          8h\nns-team-a      workload-dep-6a8b9c0d-rst34                     0/1     ImagePullBackOff   0          11h\nns-team-b      indexer-dep-7c8d9e0f-uvw56                      0/1     ImagePullBackOff   0          3h55m\nns-shared      sidecar-dep-8b9c0d1e-xyz78                      0/1     ErrImagePull       0          45m\nns-shared      batch-harness-dep-9c0d1e2f-ab901                1/1     Running            6          4h\nns-monitoring  obs-collector-dep-0d1e2f3g-cd234                1/1     Running            18         2d\n</code></pre> <p>Note</p> <p><code>Running</code> pods appear when restart count reaches the threshold (\u2265 5). The phase is healthy but the pod is worth investigating.</p>"},{"location":"examples/pods/#useful-commands","title":"Useful commands","text":"<pre><code>kubectl audit pods -A                      # all namespaces\nkubectl audit pods -n ns-prod -l app=api   # label filter\nkubectl audit pods -o wide                 # extended columns\nkubectl audit pods -o json | jq ...        # JSON for scripting\n</code></pre>"},{"location":"examples/pods/#next-steps","title":"Next steps","text":"<ul> <li>Need per-container detail? \u2192 kubectl audit containers</li> <li>Seeing high restarts? \u2192 Check Events for <code>BackOff</code> warnings</li> <li>See the full detection criteria \u2192 Audit Rules: Pods</li> </ul>"},{"location":"examples/services/","title":"Services","text":"<p><code>kubectl audit service</code> finds Services whose pod selector matches no Pods in the namespace \u2014 these Services will silently drop all traffic.</p> <p>Skipped: <code>type: ExternalName</code> (DNS-based routing, no selector) and Services with an empty selector (headless / manually managed endpoints).</p>"},{"location":"examples/services/#demo","title":"Demo","text":"<pre><code>kubectl apply -f examples/audit-svc/demo.yaml\nkubectl audit service -n kubectl-audit-svc-demo\nkubectl delete -f examples/audit-svc/demo.yaml\n</code></pre>"},{"location":"examples/services/#sample-output","title":"Sample output","text":"<pre><code>$ kubectl audit svc -A\n-------------------------------------------------------\nService Audit summary: total = 24 benign = 22 attention = 2\n-------------------------------------------------------\nService that requires attention\nNAMESPACE   NAME              TYPE        CLUSTER-IP     PORT(S)    AGE\napp-ns      broken-svc        ClusterIP   10.96.0.100    80/TCP     2h\nweb-ns      orphan-frontend   ClusterIP   10.96.0.200    443/TCP    45m\n</code></pre>"},{"location":"examples/services/#common-causes","title":"Common causes","text":"<ul> <li>Selector label mismatch \u2014 Service selects <code>app: frontend</code> but Pods are labeled <code>app: web-frontend</code></li> <li>All matching Pods were deleted or evicted</li> <li>Namespace mismatch \u2014 Pods exist in a different namespace</li> <li>Wrong namespace on the Service \u2014 Service is in <code>staging</code>, Pods are in <code>production</code></li> </ul>"},{"location":"examples/services/#useful-commands","title":"Useful commands","text":"<pre><code>kubectl audit svc -A\nkubectl audit service -n my-namespace -l app=myapp\nkubectl audit service -o json | jq -r '.items[] | \"\\(.metadata.namespace)/\\(.metadata.name)\"'\n</code></pre> <p>Note</p> <p>The <code>-l</code>/<code>--selector</code> flag filters Services, not Pods. Pod matching always uses all Pods in the namespace.</p>"},{"location":"examples/services/#next-steps","title":"Next steps","text":"<ul> <li>Found a broken service? Check the pods it should be selecting:   <pre><code>kubectl audit pods -n &lt;namespace&gt; -l &lt;service-selector-labels&gt;\n</code></pre></li> <li>See full rules \u2192 Audit Rules: Services</li> </ul>"},{"location":"examples/storage/","title":"PV &amp; PVC","text":""},{"location":"examples/storage/#persistentvolumeclaims-pvc","title":"PersistentVolumeClaims (PVC)","text":"<p><code>kubectl audit pvc</code> lists PVCs not in <code>Bound</code> phase. Pods that reference an unbound PVC will remain <code>Pending</code>.</p> <p>Flagged phases: <code>Pending</code> (not yet provisioned), <code>Lost</code> (backing PV lost or deleted)</p>"},{"location":"examples/storage/#demo","title":"Demo","text":"<pre><code>kubectl apply -f examples/audit-pvc/demo.yaml\nkubectl audit pvc -n kubectl-audit-pvc-demo\nkubectl delete -f examples/audit-pvc/demo.yaml\n</code></pre>"},{"location":"examples/storage/#sample-output","title":"Sample output","text":"<pre><code>$ kubectl audit pvc -A\n-------------------------------------------------------\nPersistentVolumeClaim Audit summary: total = 45 benign = 43 attention = 2\n-------------------------------------------------------\nPersistentVolumeClaim that requires attention\nNAMESPACE    NAME              STATUS    VOLUME\napp-demo     logs-claim-01     Pending\ndata-demo    backup-claim-02   Lost\n</code></pre>"},{"location":"examples/storage/#persistentvolumes-pv","title":"PersistentVolumes (PV)","text":"<p><code>kubectl audit pv</code> lists PVs not in <code>Bound</code> phase. PVs are cluster-scoped; <code>-n</code> and <code>-A</code> do not apply.</p> <p>Flagged phases: <code>Available</code> (no claim bound), <code>Released</code> (claim deleted, awaiting reclaim), <code>Failed</code> (reclaim failure)</p>"},{"location":"examples/storage/#demo_1","title":"Demo","text":"<pre><code>kubectl apply -f examples/audit-pv/demo.yaml\nkubectl audit pv\nkubectl delete -f examples/audit-pv/demo.yaml\n</code></pre>"},{"location":"examples/storage/#sample-output_1","title":"Sample output","text":"<pre><code>$ kubectl audit pv\n-------------------------------------------------------\nPersistentVolume Audit summary: total = 12 benign = 11 attention = 1\n-------------------------------------------------------\nPersistentVolume that requires attention\nNAME              CAPACITY   ACCESS MODES   STATUS      CLAIM\npv-archive-001    500Gi      RWO            Released    demo-ns/pvc-old-claim\n</code></pre>"},{"location":"examples/storage/#useful-commands","title":"Useful commands","text":"<pre><code>kubectl audit pvc -A                 # PVCs across all namespaces\nkubectl audit pv                     # PVs (cluster-scoped)\nkubectl audit pvc -o json | jq ...   # JSON for scripting\n</code></pre>"},{"location":"examples/storage/#next-steps","title":"Next steps","text":"<ul> <li>See detection rules \u2192 Audit Rules: PVC / PV</li> <li>A <code>Pending</code> PVC means Pods using it will be stuck in <code>Pending</code> too \u2192 check with <code>kubectl audit pods -A</code></li> </ul>"},{"location":"getting-started/","title":"Getting Started","text":"<p>New to <code>kubectl-audit</code>? Start here.</p> <ol> <li>Install \u2014 add the plugin via Krew or build from source</li> <li>Quick Start \u2014 run your first audit in 60 seconds</li> <li>RBAC &amp; Permissions \u2014 minimum cluster permissions needed</li> </ol> <p>Once installed, the Subcommands reference and Audit Rules cover everything the plugin checks and why.</p>"},{"location":"getting-started/install/","title":"Installation","text":""},{"location":"getting-started/install/#requirements","title":"Requirements","text":"<ul> <li>kubectl 1.20+</li> <li>Kubernetes cluster 1.21+ (uses <code>batch/v1</code>, <code>apps/v1</code>, <code>core/v1</code>, <code>events.k8s.io/v1</code>)</li> <li>Krew (recommended install method)</li> </ul>"},{"location":"getting-started/install/#krew-recommended","title":"Krew \u2014 recommended","text":"<p><code>kubectl-audit</code> is published through the codenio Krew index.</p> <pre><code>kubectl krew index add codenio https://github.com/codenio/codenio-krew-index.git\nkubectl krew install codenio/audit\n</code></pre> <p>Index name conflict</p> <p>If the name <code>codenio</code> is already taken in your Krew config, choose any other name for the <code>index add</code> step: <pre><code>kubectl krew index add my-codenio https://github.com/codenio/codenio-krew-index.git\nkubectl krew install my-codenio/audit\n</code></pre> Upgrade stays the same regardless of index name: <pre><code>kubectl krew upgrade audit\n</code></pre></p>"},{"location":"getting-started/install/#krew-default-index","title":"Krew default index","text":"<p>If the plugin is listed in Krew's default index:</p> <pre><code>kubectl krew install audit\n</code></pre>"},{"location":"getting-started/install/#from-source","title":"From source","text":"<p>Requires Go 1.21+ and <code>make</code>.</p> <pre><code>git clone https://github.com/codenio/kubectl-audit.git\ncd kubectl-audit\nmake install\n</code></pre> <p>Builds <code>bin/audit</code> and copies it to <code>~/.krew/bin/kubectl-audit</code>. Ensure <code>~/.krew/bin</code> is on your <code>PATH</code>.</p>"},{"location":"getting-started/install/#verify","title":"Verify","text":"<pre><code>kubectl audit --help\n</code></pre> <p>You should see the plugin's help output listing all subcommands.</p> <p>Next: Quick Start \u2192</p>"},{"location":"getting-started/quickstart/","title":"Quick Start","text":"<p>After installing, run your first audit:</p> <pre><code>kubectl audit pods\n</code></pre>"},{"location":"getting-started/quickstart/#choose-the-right-subcommand","title":"Choose the right subcommand","text":"I want to\u2026 Command See all unhealthy pods <code>kubectl audit pods -A</code> Drill into a specific pod's containers <code>kubectl audit containers -p &lt;pod-name&gt;</code> Check which nodes can't schedule work <code>kubectl audit nodes</code> Find services with no backing pods <code>kubectl audit svc -A</code> Check deployment rollout health <code>kubectl audit deploy -A</code> Catch warning events cluster-wide <code>kubectl audit events -A</code> Check storage (PVC/PV) issues <code>kubectl audit pvc -A &amp;&amp; kubectl audit pv</code> <p>pods vs containers</p> <ul> <li><code>kubectl audit pods</code> \u2014 one row per pod, aggregate status</li> <li><code>kubectl audit containers</code> \u2014 one row per container, shows exact image/restart detail</li> </ul> <p>Start with <code>pods</code> for a quick count. Use <code>containers</code> when you need to identify which container inside a multi-container pod is broken.</p>"},{"location":"getting-started/quickstart/#common-workflows","title":"Common workflows","text":""},{"location":"getting-started/quickstart/#incident-triage-cluster-wide-sweep","title":"Incident triage \u2014 cluster-wide sweep","text":"<pre><code>kubectl audit pods -A\nkubectl audit nodes\nkubectl audit events -A\n</code></pre>"},{"location":"getting-started/quickstart/#pre-deploy-smoke-check","title":"Pre-deploy smoke check","text":"<pre><code>kubectl audit pods -n my-namespace\nkubectl audit deploy -n my-namespace\nkubectl audit svc -n my-namespace\n</code></pre>"},{"location":"getting-started/quickstart/#scope-to-a-team-namespace-with-label-filter","title":"Scope to a team namespace with label filter","text":"<pre><code>kubectl audit pods -n team-a -l app=api\nkubectl audit containers -n team-a -l app=api\n</code></pre>"},{"location":"getting-started/quickstart/#try-a-demo-no-broken-cluster-needed","title":"Try a demo (no broken cluster needed)","text":"<p>Each audit type has a demo manifest that creates intentionally broken resources:</p> PodsServicesEvents <pre><code>kubectl apply -f https://raw.githubusercontent.com/codenio/kubectl-audit/main/examples/audit-pods/demo.yaml\nkubectl audit pods -n kubectl-audit-pods-demo\nkubectl delete -f https://raw.githubusercontent.com/codenio/kubectl-audit/main/examples/audit-pods/demo.yaml\n</code></pre> <pre><code>kubectl apply -f https://raw.githubusercontent.com/codenio/kubectl-audit/main/examples/audit-svc/demo.yaml\nkubectl audit service -n kubectl-audit-svc-demo\nkubectl delete -f https://raw.githubusercontent.com/codenio/kubectl-audit/main/examples/audit-svc/demo.yaml\n</code></pre> <pre><code>kubectl apply -f https://raw.githubusercontent.com/codenio/kubectl-audit/main/examples/audit-events/demo.yaml\nkubectl audit events -n kubectl-audit-events-demo\nkubectl delete -f https://raw.githubusercontent.com/codenio/kubectl-audit/main/examples/audit-events/demo.yaml\n</code></pre> <p>See all demo examples \u2192</p>"},{"location":"getting-started/quickstart/#scripting-and-cicd","title":"Scripting and CI/CD","text":"<p>The audit summary is written to stderr, so stdout is clean for piping:</p> <pre><code># Extract names of failing pods as JSON\nkubectl audit pods -o json 2&gt;/dev/null | jq '[.items[].metadata.name]'\n\n# Non-zero exit if any unhealthy pods found\nkubectl audit pods -n my-namespace -o name | grep -q . &amp;&amp; echo \"Issues found\"\n</code></pre> <p>See the full CI/CD integration guide \u2192</p> <p>Next: RBAC &amp; Permissions \u2192</p>"},{"location":"getting-started/rbac/","title":"RBAC &amp; Permissions","text":"<p><code>kubectl-audit</code> is a read-only plugin \u2014 it never creates, patches, or deletes resources. It uses <code>list</code> and <code>get</code> on the following API groups.</p>"},{"location":"getting-started/rbac/#minimum-clusterrole","title":"Minimum ClusterRole","text":"<p>Apply this to grant the minimum permissions needed to run all audit subcommands:</p> kubectl-audit-reader.yaml<pre><code>apiVersion: rbac.authorization.k8s.io/v1\nkind: ClusterRole\nmetadata:\n  name: kubectl-audit-reader\nrules:\n  - apiGroups: [\"\"]\n    resources:\n      - pods\n      - nodes\n      - persistentvolumes\n      - persistentvolumeclaims\n      - services\n      - events\n    verbs: [\"get\", \"list\"]\n  - apiGroups: [\"apps\"]\n    resources:\n      - deployments\n    verbs: [\"get\", \"list\"]\n  - apiGroups: [\"batch\"]\n    resources:\n      - jobs\n      - cronjobs\n    verbs: [\"get\", \"list\"]\n---\napiVersion: rbac.authorization.k8s.io/v1\nkind: ClusterRoleBinding\nmetadata:\n  name: kubectl-audit-reader-binding\nsubjects:\n  - kind: User\n    name: &lt;your-username&gt;     # replace with your user or ServiceAccount\n    apiGroup: rbac.authorization.k8s.io\nroleRef:\n  kind: ClusterRole\n  name: kubectl-audit-reader\n  apiGroup: rbac.authorization.k8s.io\n</code></pre> <pre><code>kubectl apply -f kubectl-audit-reader.yaml\n</code></pre>"},{"location":"getting-started/rbac/#per-subcommand-permissions","title":"Per-subcommand permissions","text":"<p>If you want to grant access only to specific audits:</p> Subcommand API group Resource <code>pods</code> <code>\"\"</code> (core) <code>pods</code> <code>containers</code> <code>\"\"</code> (core) <code>pods</code> <code>nodes</code> <code>\"\"</code> (core) <code>nodes</code> <code>pvc</code> <code>\"\"</code> (core) <code>persistentvolumeclaims</code> <code>pv</code> <code>\"\"</code> (core) <code>persistentvolumes</code> <code>service</code> <code>\"\"</code> (core) <code>services</code>, <code>pods</code> <code>events</code> <code>\"\"</code> (core) <code>events</code> <code>deploy</code> <code>apps</code> <code>deployments</code> <code>jobs</code> <code>batch</code> <code>jobs</code> <code>cronjobs</code> <code>batch</code> <code>cronjobs</code> <p><code>service</code> requires pods</p> <p>The <code>service</code> audit needs to list Pods to check if any match each Service's selector. Granting only <code>services</code> list will cause the audit to fail.</p>"},{"location":"getting-started/rbac/#checking-your-current-permissions","title":"Checking your current permissions","text":"<pre><code>kubectl auth can-i list pods --all-namespaces\nkubectl auth can-i list nodes\nkubectl auth can-i list deployments --all-namespaces\n</code></pre> <p>If <code>kubectl audit</code> returns a permission error, check access for the specific resource the subcommand queries.</p>"},{"location":"guides/cicd/","title":"CI/CD Integration","text":"<p><code>kubectl-audit</code> works well as a pre-deploy gate or post-deploy health check. The audit summary goes to stderr; stdout is clean JSON/YAML for scripting.</p>"},{"location":"guides/cicd/#exit-codes","title":"Exit codes","text":"Exit code Meaning <code>0</code> Command ran successfully (even if issues were found \u2014 the plugin reports, not enforces) non-zero Command failed (API error, permission denied, invalid flag) <p>Enforcing as a gate</p> <p><code>kubectl-audit</code> returns exit code <code>0</code> even when issues are found \u2014 it is a reporter, not a policy enforcer. To gate on findings, check the count via <code>-o json</code> and <code>jq</code> (see examples below).</p>"},{"location":"guides/cicd/#shell-scripting","title":"Shell scripting","text":""},{"location":"guides/cicd/#gate-on-pod-health","title":"Gate on pod health","text":"<pre><code># Fail the script if any unhealthy pods exist in a namespace\nISSUES=$(kubectl audit pods -n my-namespace -o json 2&gt;/dev/null | jq '.items | length')\nif [ \"$ISSUES\" -gt 0 ]; then\n  echo \"\u274c $ISSUES unhealthy pod(s) found\"\n  kubectl audit pods -n my-namespace\n  exit 1\nfi\necho \"\u2705 All pods healthy\"\n</code></pre>"},{"location":"guides/cicd/#post-deploy-check","title":"Post-deploy check","text":"<pre><code># Wait a few seconds after deploy, then audit\nsleep 15\nkubectl audit pods -n production -o json 2&gt;/dev/null \\\n  | jq -r '.items[] | \"\\(.metadata.namespace)/\\(.metadata.name): \\(.status.phase)\"'\n</code></pre>"},{"location":"guides/cicd/#github-actions","title":"GitHub Actions","text":""},{"location":"guides/cicd/#pre-deploy-gate","title":"Pre-deploy gate","text":".github/workflows/deploy.yml<pre><code>name: deploy\non:\n  push:\n    branches: [main]\n\njobs:\n  pre-deploy-audit:\n    runs-on: ubuntu-latest\n    steps:\n      - name: Set up kubectl\n        uses: azure/setup-kubectl@v4\n\n      - name: Install kubectl-audit\n        run: |\n          kubectl krew index add codenio https://github.com/codenio/codenio-krew-index.git\n          kubectl krew install codenio/audit\n\n      - name: Pre-deploy health check\n        env:\n          KUBECONFIG: ${{ secrets.KUBECONFIG }}\n        run: |\n          ISSUES=$(kubectl audit pods -n production -o json 2&gt;/dev/null | jq '.items | length')\n          if [ \"$ISSUES\" -gt 0 ]; then\n            echo \"::error::$ISSUES unhealthy pod(s) in production \u2014 aborting deploy\"\n            kubectl audit pods -n production\n            exit 1\n          fi\n\n  deploy:\n    needs: pre-deploy-audit\n    runs-on: ubuntu-latest\n    steps:\n      - name: Deploy\n        run: echo \"Deploying...\"\n</code></pre>"},{"location":"guides/cicd/#post-deploy-audit-comment-on-pr","title":"Post-deploy audit comment on PR","text":".github/workflows/audit-comment.yml<pre><code>name: audit-comment\non:\n  pull_request:\n    types: [opened, synchronize]\n\njobs:\n  audit:\n    runs-on: ubuntu-latest\n    steps:\n      - name: Install kubectl-audit\n        run: |\n          kubectl krew index add codenio https://github.com/codenio/codenio-krew-index.git\n          kubectl krew install codenio/audit\n\n      - name: Run audit\n        id: audit\n        env:\n          KUBECONFIG: ${{ secrets.KUBECONFIG }}\n        run: |\n          OUTPUT=$(kubectl audit pods -A 2&gt;&amp;1)\n          echo \"result&lt;&lt;EOF\" &gt;&gt; $GITHUB_OUTPUT\n          echo \"$OUTPUT\" &gt;&gt; $GITHUB_OUTPUT\n          echo \"EOF\" &gt;&gt; $GITHUB_OUTPUT\n\n      - name: Comment on PR\n        uses: actions/github-script@v7\n        with:\n          script: |\n            github.rest.issues.createComment({\n              issue_number: context.issue.number,\n              owner: context.repo.owner,\n              repo: context.repo.repo,\n              body: `## Cluster Audit\\n\\`\\`\\`\\n${{ steps.audit.outputs.result }}\\n\\`\\`\\``\n            })\n</code></pre>"},{"location":"guides/cicd/#gitlab-ci","title":"GitLab CI","text":".gitlab-ci.yml<pre><code>audit:\n  stage: validate\n  image: bitnami/kubectl:latest\n  before_script:\n    - kubectl krew index add codenio https://github.com/codenio/codenio-krew-index.git\n    - kubectl krew install codenio/audit\n  script:\n    - |\n      ISSUES=$(kubectl audit pods -n $CI_ENVIRONMENT_SLUG -o json 2&gt;/dev/null | jq '.items | length')\n      if [ \"$ISSUES\" -gt 0 ]; then\n        kubectl audit pods -n $CI_ENVIRONMENT_SLUG\n        exit 1\n      fi\n  environment:\n    name: production\n</code></pre>"},{"location":"guides/cicd/#parsing-output-with-jq","title":"Parsing output with jq","text":"<pre><code># Names of unhealthy pods\nkubectl audit pods -A -o json 2&gt;/dev/null | jq -r '.items[].metadata.name'\n\n# Pods grouped by namespace\nkubectl audit pods -A -o json 2&gt;/dev/null \\\n  | jq 'group_by(.metadata.namespace) | map({namespace: .[0].metadata.namespace, count: length})'\n\n# Services with no backing pods\nkubectl audit service -A -o json 2&gt;/dev/null \\\n  | jq -r '.items[] | \"\\(.metadata.namespace)/\\(.metadata.name)\"'\n</code></pre>"},{"location":"guides/troubleshooting/","title":"Troubleshooting","text":""},{"location":"guides/troubleshooting/#plugin-not-found","title":"Plugin not found","text":"<p>Error: <pre><code>error: unknown command \"audit\" for \"kubectl\"\n</code></pre></p> <p>Causes and fixes:</p> <ol> <li>Plugin not installed \u2014 run <code>kubectl krew install codenio/audit</code></li> <li><code>~/.krew/bin</code> not on <code>PATH</code>:    <pre><code>export PATH=\"${KREW_ROOT:-$HOME/.krew}/bin:$PATH\"\n</code></pre>    Add this to your shell profile (<code>~/.zshrc</code>, <code>~/.bashrc</code>).</li> <li>Verify the binary exists:    <pre><code>ls ~/.krew/bin/kubectl-audit\nkubectl audit --help\n</code></pre></li> </ol>"},{"location":"guides/troubleshooting/#empty-output-nothing-flagged","title":"Empty output \u2014 nothing flagged","text":"<p>If <code>kubectl audit pods</code> shows <code>attention = 0</code> but you expect issues:</p> <ol> <li>Your pods are actually healthy \u2014 the audit rules are conservative. Check Audit Rules for exact thresholds.</li> <li>Wrong namespace \u2014 add <code>-A</code> to check all namespaces:    <pre><code>kubectl audit pods -A\n</code></pre></li> <li>Wrong context \u2014 verify you're on the right cluster:    <pre><code>kubectl config current-context\nkubectl audit pods -A --context=my-cluster\n</code></pre></li> </ol>"},{"location":"guides/troubleshooting/#permission-denied-forbidden","title":"Permission denied / Forbidden","text":"<p>Error: <pre><code>Error from server (Forbidden): pods is forbidden: User \"...\" cannot list resource \"pods\"\n</code></pre></p> <p>You need read access to the resources being audited. See RBAC &amp; Permissions for the minimum ClusterRole.</p> <p>Quick check: <pre><code>kubectl auth can-i list pods --all-namespaces\nkubectl auth can-i list nodes\n</code></pre></p>"},{"location":"guides/troubleshooting/#unexpected-output-for-containers","title":"Unexpected output for <code>containers</code>","text":"<p><code>-o custom-columns</code> not working: The <code>containers</code> subcommand uses a custom printer \u2014 it does not support <code>custom-columns</code>, Go templates, or <code>jsonpath</code>. Use <code>-o json</code> or <code>-o yaml</code> for machine output, then pipe to <code>jq</code>.</p> <pre><code>kubectl audit containers -o json | jq '.items[] | {pod: .pod, name: .name, status: .status}'\n</code></pre>"},{"location":"guides/troubleshooting/#suspended-cronjobs-not-appearing","title":"Suspended CronJobs not appearing","text":"<p><code>kubectl audit cronjobs</code> only flags CronJobs where <code>spec.suspend = true</code>. If your CronJob is failing (not suspended), check Jobs instead:</p> <pre><code>kubectl audit jobs -A\n</code></pre>"},{"location":"guides/troubleshooting/#service-shows-as-healthy-but-traffic-isnt-routing","title":"Service shows as healthy but traffic isn't routing","text":"<p><code>kubectl audit service</code> only checks whether the Service's selector matches any Pods \u2014 it doesn't verify those Pods are <code>Ready</code> or actually serving traffic. After confirming the Service is matched:</p> <pre><code>kubectl audit pods -n &lt;namespace&gt; -l &lt;service-selector&gt;\nkubectl audit containers -n &lt;namespace&gt; -l &lt;service-selector&gt;\n</code></pre>"},{"location":"guides/troubleshooting/#output-looks-different-from-docs","title":"Output looks different from docs","text":"<p>The plugin uses the same column widths and formats as <code>kubectl get</code>. Column alignment can differ depending on terminal width and the length of names in your cluster. The audit summary line is always on stderr; redirect if needed:</p> <pre><code>kubectl audit pods 2&gt;/dev/null    # suppress summary\nkubectl audit pods 2&gt;&amp;1           # merge to stdout\n</code></pre>"},{"location":"guides/troubleshooting/#filing-a-bug","title":"Filing a bug","text":"<p>If you hit an issue not covered here, open an issue at github.com/codenio/kubectl-audit/issues with:</p> <ul> <li><code>kubectl audit --version</code> output (or binary version)</li> <li><code>kubectl version</code> output</li> <li>The exact command you ran and the full error</li> </ul>"},{"location":"reference/audit-rules/","title":"Audit Rules &amp; Detection Logic","text":"<p>This page is the single authoritative reference for every rule <code>kubectl-audit</code> applies. Rules are fixed in code \u2014 there are no flags to adjust thresholds.</p>"},{"location":"reference/audit-rules/#pods","title":"Pods","text":"<p>A pod is flagged if any of the following are true:</p> Rule Detail Phase \u2260 <code>Running</code> Includes <code>Pending</code>, <code>Failed</code>, <code>Unknown</code>. <code>Succeeded</code>/<code>Completed</code> job pods are also flagged \u2014 they are not in a running steady state. Any regular container <code>Ready = false</code> Checked via <code>containerStatuses[*].ready</code>. Init containers are not checked for readiness here (see Containers). Any container <code>restartCount \u2265 5</code> Applies to both regular and init containers. The threshold is 5 and is not configurable. <p>Why are Succeeded pods flagged?</p> <p><code>Succeeded</code> pods from completed Jobs are intentionally included \u2014 they consume namespace resources and are often cleaned up by a TTL controller. Their presence signals they haven't been collected.</p>"},{"location":"reference/audit-rules/#containers","title":"Containers","text":"<p>Each container row (init or app) is flagged if:</p> Rule Waiting reasons that trigger Container is in <code>Waiting</code> state with a known error reason <code>CrashLoopBackOff</code>, <code>ImagePullBackOff</code>, <code>ErrImagePull</code>, <code>InvalidImageName</code>, <code>CreateContainerConfigError</code>, <code>RunContainerError</code> Container <code>ready = false</code> and pod is not in a terminal phase Pod is still expected to be running <code>restartCount \u2265 5</code> Same threshold as pod-level Init container in <code>Failed</code> state <code>lastState.terminated.exitCode \u2260 0</code>"},{"location":"reference/audit-rules/#nodes","title":"Nodes","text":"<p>A node is flagged if any condition is true:</p> Rule Detail <code>Ready</code> condition = <code>False</code> or <code>Unknown</code> The <code>NodeReady</code> condition is not True <code>spec.unschedulable = true</code> Node is cordoned \u2014 <code>kubectl cordon</code> sets this flag; shown as <code>SchedulingDisabled</code>"},{"location":"reference/audit-rules/#persistentvolumeclaims-pvc","title":"PersistentVolumeClaims (PVC)","text":"Rule Flagged phases Phase \u2260 <code>Bound</code> <code>Pending</code> (not yet provisioned), <code>Lost</code> (backing PV lost)"},{"location":"reference/audit-rules/#persistentvolumes-pv","title":"PersistentVolumes (PV)","text":"Rule Flagged phases Phase \u2260 <code>Bound</code> <code>Available</code> (no claim), <code>Released</code> (claim deleted, not yet reclaimed), <code>Failed</code> (reclaim failure)"},{"location":"reference/audit-rules/#jobs","title":"Jobs","text":"Rule Detail <code>status.failed &gt; 0</code> Any failed attempt, including backoff limit reached and active deadline exceeded"},{"location":"reference/audit-rules/#cronjobs","title":"CronJobs","text":"Rule Detail <code>spec.suspend = true</code> CronJob is suspended \u2014 no new Jobs will be created"},{"location":"reference/audit-rules/#services","title":"Services","text":"Rule Detail Selector has entries and no Pods match The Service's <code>spec.selector</code> is non-empty, but no Pods in the same namespace have all those labels Skipped: <code>type: ExternalName</code> These route by DNS name, not Pod selector Skipped: empty <code>spec.selector</code> Headless services or manually-managed endpoints <p>Tip</p> <p>The <code>-l</code>/<code>--selector</code> flag for <code>kubectl audit service</code> filters the Service list, not the Pod matching. Pod matching always uses all Pods in each namespace.</p>"},{"location":"reference/audit-rules/#deployments","title":"Deployments","text":"<p>A deployment is flagged if either condition is true:</p> Rule Detail <code>spec.replicas == 0</code> Explicitly scaled to zero <code>status.readyReplicas &lt; desired</code> when <code>desired &gt; 0</code> <code>desired</code> = <code>spec.replicas</code> if set, otherwise 1 (Kubernetes API default when <code>spec.replicas</code> is <code>nil</code>)"},{"location":"reference/audit-rules/#events","title":"Events","text":"Rule Detail <code>type = Warning</code> All Warning events regardless of reason or source Excluded: <code>type = Normal</code> Normal events are informational; only Warnings indicate problems"},{"location":"reference/flags/","title":"Flags Reference","text":"<p><code>kubectl-audit</code> uses standard <code>kubectl</code> configuration for context, namespace, and kubeconfig. All global flags accepted by <code>kubectl</code> (e.g., <code>--kubeconfig</code>, <code>--context</code>, <code>-n</code>) apply.</p>"},{"location":"reference/flags/#global-flags-all-subcommands","title":"Global flags (all subcommands)","text":"Flag Default Description <code>--context</code> current context Kubeconfig context to use <code>--kubeconfig</code> <code>~/.kube/config</code> Path to kubeconfig file <code>-n</code>, <code>--namespace</code> current namespace Target namespace for namespaced resources <code>-o</code>, <code>--output</code> table Output format (see Output Formats) <code>-v</code>, <code>--v</code> 0 Log verbosity level"},{"location":"reference/flags/#namespace-flags","title":"Namespace flags","text":"Flag Short Description <code>--all-namespaces</code> <code>-A</code> List across all namespaces (namespaced subcommands only) <code>--namespace</code> <code>-n</code> Target a specific namespace"},{"location":"reference/flags/#selector-flag","title":"Selector flag","text":"Flag Short Description <code>--selector</code> <code>-l</code> Label selector (e.g., <code>app=web</code>, <code>env in (prod,staging)</code>) <p>For <code>containers</code>: applies to the Pod list. For <code>service</code>: applies to the Service list. For <code>deploy</code>: applies to the Deployment list.</p>"},{"location":"reference/flags/#containers-specific-flag","title":"containers-specific flag","text":"Flag Short Description <code>--pod</code> <code>-p</code> Filter to an exact pod <code>metadata.name</code>"},{"location":"reference/flags/#getting-help","title":"Getting help","text":"<pre><code>kubectl audit --help\nkubectl audit &lt;subcommand&gt; --help\n</code></pre>"},{"location":"reference/output-formats/","title":"Output Formats","text":""},{"location":"reference/output-formats/#standard-subcommands","title":"Standard subcommands","text":"<p>For <code>pods</code>, <code>nodes</code>, <code>pv</code>, <code>pvc</code>, <code>jobs</code>, <code>cronjobs</code>, <code>service</code>, <code>deploy</code>, and <code>events</code>, the <code>-o</code> flag works identically to <code>kubectl get</code>:</p> <pre><code>kubectl audit pods -o wide\nkubectl audit nodes -o json\nkubectl audit pvc -o yaml\nkubectl audit jobs -o custom-columns=NAME:.metadata.name\nkubectl audit service -o json\nkubectl audit events -o wide\nkubectl audit deploy -o jsonpath='{.items[*].metadata.name}'\n</code></pre>"},{"location":"reference/output-formats/#containers-subcommand","title":"containers subcommand","text":"<p>The <code>containers</code> subcommand uses a dedicated printer with limited format support:</p> Format Output (default) Table: <code>POD</code>, <code>NAME</code>, <code>READY</code>, <code>STATUS</code>, <code>RESTARTS</code>, <code>AGE</code>, <code>TYPE</code> <code>-o wide</code> Adds <code>PORTS</code>, <code>IMAGE</code>, <code>PULLPOLICY</code> <code>-o json</code> Full JSON <code>-o yaml</code> Full YAML <code>-o name</code> <code>namespace/pod-name</code> lines <p>Unsupported formats for <code>containers</code></p> <p><code>custom-columns</code>, <code>jsonpath</code>, and Go templates are not supported and will return a clear error. Use <code>-o json | jq</code> instead: <pre><code>kubectl audit containers -o json | jq '.items[] | {pod: .pod, name: .name, status: .status}'\n</code></pre></p>"},{"location":"reference/output-formats/#audit-summary-line","title":"Audit summary line","text":"<p>Every audit writes a summary to stderr, keeping stdout clean for piping:</p> <pre><code>Pod Audit summary: total = 120 benign = 114 attention = 6\n</code></pre> <p>Redirect as needed:</p> <pre><code>kubectl audit pods -o json 2&gt;/dev/null | jq ...    # suppress summary\nkubectl audit pods 2&gt;&amp;1 | tee audit.log             # capture both\n</code></pre>"},{"location":"reference/output-formats/#custom-columns-example","title":"Custom columns example","text":"<pre><code>kubectl audit deploy -o custom-columns=\\\nNAME:.metadata.name,\\\nNAMESPACE:.metadata.namespace,\\\nDESIRED:.spec.replicas,\\\nREADY:.status.readyReplicas\n</code></pre>"},{"location":"reference/output-formats/#machine-readable-scripting","title":"Machine-readable scripting","text":"<pre><code># Count unhealthy pods\nkubectl audit pods -A -o json 2&gt;/dev/null | jq '.items | length'\n\n# Names of flagged services\nkubectl audit service -A -o json 2&gt;/dev/null \\\n  | jq -r '.items[] | \"\\(.metadata.namespace)/\\(.metadata.name)\"'\n</code></pre> <p>See the CI/CD integration guide for complete scripting patterns.</p>"},{"location":"reference/subcommands/","title":"Subcommands Reference","text":""},{"location":"reference/subcommands/#subcommand-overview","title":"Subcommand overview","text":"Subcommand Aliases Scope <code>pods</code> <code>pod</code>, <code>po</code> Namespaced <code>containers</code> <code>container</code> Namespaced <code>nodes</code> <code>node</code>, <code>no</code> Cluster-wide <code>pvc</code> <code>pvcs</code>, <code>persistentvolumeclaim</code>, <code>persistentvolumeclaims</code> Namespaced <code>pv</code> <code>pvs</code>, <code>persistentvolume</code>, <code>persistentvolumes</code> Cluster-wide <code>jobs</code> <code>job</code> Namespaced <code>cronjobs</code> <code>cronjob</code>, <code>cj</code> Namespaced <code>service</code> <code>services</code>, <code>svc</code> Namespaced <code>deploy</code> <code>deployment</code>, <code>deployments</code> Namespaced <code>events</code> <code>event</code>, <code>ev</code> Namespaced <p>For exact detection rules per subcommand, see Audit Rules.</p>"},{"location":"reference/subcommands/#pods","title":"pods","text":"<p>Lists pods that are not in a healthy steady state.</p> <pre><code>kubectl audit pods [-n NAMESPACE] [-A] [-l SELECTOR] [-o FORMAT]\n</code></pre> <p>Flagged when: phase \u2260 <code>Running</code>, any container not <code>Ready</code>, or restart count \u2265 5. <code>Succeeded</code>/<code>Completed</code> pods are included.</p> <p>Default columns: <code>NAME</code>, <code>READY</code>, <code>STATUS</code>, <code>RESTARTS</code>, <code>AGE</code> With <code>-A</code>: prepends <code>NAMESPACE</code> column (same as <code>kubectl get pods -A</code>)</p>"},{"location":"reference/subcommands/#containers","title":"containers","text":"<p>Per-container rows \u2014 finer detail than <code>pods</code>.</p> <pre><code>kubectl audit containers [-n NAMESPACE] [-A] [-l SELECTOR] [-p POD] [-o FORMAT]\n</code></pre> <p>Flagged when: image pull errors, crash-loop waiting states, not ready, restart count \u2265 5, failed init containers.</p> <p>Default columns: <code>POD</code>, <code>NAME</code>, <code>READY</code>, <code>STATUS</code>, <code>RESTARTS</code>, <code>AGE</code>, <code>TYPE</code> With <code>-o wide</code>: adds <code>PORTS</code>, <code>IMAGE</code>, <code>PULLPOLICY</code></p> <p>pods vs containers</p> <p>Use <code>pods</code> for a quick count. Use <code>containers</code> when you need to pinpoint which container inside a multi-container pod is broken.</p>"},{"location":"reference/subcommands/#nodes","title":"nodes","text":"<p>Lists nodes that cannot be scheduled to.</p> <pre><code>kubectl audit nodes [-o FORMAT]\n</code></pre> <p>Flagged when: <code>NotReady</code> or <code>SchedulingDisabled</code> (cordoned via <code>kubectl cordon</code>). Nodes are cluster-scoped \u2014 <code>-n</code> and <code>-A</code> do not apply.</p>"},{"location":"reference/subcommands/#pvc","title":"pvc","text":"<p>Lists PersistentVolumeClaims not in <code>Bound</code> phase.</p> <pre><code>kubectl audit pvc [-n NAMESPACE] [-A] [-o FORMAT]\n</code></pre> <p>Flagged phases: <code>Pending</code>, <code>Lost</code></p>"},{"location":"reference/subcommands/#pv","title":"pv","text":"<p>Lists PersistentVolumes not in <code>Bound</code> phase.</p> <pre><code>kubectl audit pv [-o FORMAT]\n</code></pre> <p>Flagged phases: <code>Available</code>, <code>Released</code>, <code>Failed</code> PVs are cluster-scoped \u2014 <code>-n</code> and <code>-A</code> do not apply.</p>"},{"location":"reference/subcommands/#jobs","title":"jobs","text":"<p>Lists failed Jobs.</p> <pre><code>kubectl audit jobs [-n NAMESPACE] [-A] [-l SELECTOR] [-o FORMAT]\n</code></pre> <p>Flagged when: <code>status.failed &gt; 0</code> (includes backoff limit reached, active deadline exceeded).</p>"},{"location":"reference/subcommands/#cronjobs","title":"cronjobs","text":"<p>Lists suspended CronJobs.</p> <pre><code>kubectl audit cronjobs [-n NAMESPACE] [-A] [-l SELECTOR] [-o FORMAT]\n</code></pre> <p>Flagged when: <code>spec.suspend = true</code></p>"},{"location":"reference/subcommands/#service","title":"service","text":"<p>Lists Services whose selector matches no Pods.</p> <pre><code>kubectl audit service [-n NAMESPACE] [-A] [-l SELECTOR] [-o FORMAT]\n</code></pre> <p>Flagged when: non-empty <code>spec.selector</code> with no matching Pods in the namespace. Skipped: <code>type: ExternalName</code>, empty selectors.</p> <p>Note</p> <p><code>-l</code>/<code>--selector</code> filters the Service list. Pod matching always uses all Pods in the namespace.</p>"},{"location":"reference/subcommands/#deploy","title":"deploy","text":"<p>Lists Deployments that are scaled down or have unhealthy replicas.</p> <pre><code>kubectl audit deploy [-n NAMESPACE] [-A] [-l SELECTOR] [-o FORMAT]\n</code></pre> <p>Flagged when: <code>spec.replicas == 0</code>, or <code>readyReplicas &lt; desired</code> when <code>desired &gt; 0</code>. <code>desired</code> defaults to 1 when <code>spec.replicas</code> is unset (matches Kubernetes API default).</p>"},{"location":"reference/subcommands/#events","title":"events","text":"<p>Lists Warning events.</p> <pre><code>kubectl audit events [-n NAMESPACE] [-A] [-l SELECTOR] [-o FORMAT]\n</code></pre> <p>Flagged when: <code>type = Warning</code>. Normal events are excluded.</p>"},{"location":"reference/subcommands/#common-flags","title":"Common flags","text":"Flag Short Applies to Description <code>--all-namespaces</code> <code>-A</code> Namespaced targets List across all namespaces <code>--namespace</code> <code>-n</code> Namespaced targets Target namespace <code>--selector</code> <code>-l</code> All Label selector (e.g., <code>app=web</code>) <code>--output</code> <code>-o</code> All Output format (see Output Formats) <code>--context</code> All Kubeconfig context override <code>--kubeconfig</code> All Path to kubeconfig file <code>--pod</code> <code>-p</code> <code>containers</code> only Filter to a single pod by exact name"},{"location":"reference/subcommands/#getting-help","title":"Getting help","text":"<pre><code>kubectl audit --help\nkubectl audit &lt;subcommand&gt; --help\n</code></pre>"}]}